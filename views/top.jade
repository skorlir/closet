header
	a(href='/profile')
		if(user)
			img.profile(src=user.profilePicture)
			h3=user.name.first
	a(href='/')
		span.centeredLogo Out Side
	button.searchButton Search
	input(type='search', placeholder='search for a user', name='searchField')
nav
	ul
		li#my-hobbies
			h3 MY HOBBIES
		li#my-closet
			h3 MY CLOSET
		li#explore
			h3 EXPLORE